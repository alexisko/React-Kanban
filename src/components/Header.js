import React from 'react';

const Header = () => {
  return (
    <nav className="navbar navbar-inverse navbar-fixed-top">
      <div className="navbar-header">
        <a className="navbar-brand pull-left" href="#">React Kanban</a>
      </div>
    </nav>
  );
};

export default Header;